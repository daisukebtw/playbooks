---

stages:
  - lint

image: registry.gitlab.com/daisukebtw/playbooks

lint_job:
  stage: lint
  variables:
    LANG: C.UTF-8
    LC_ALL: C.UTF-8
    ANSIBLE_CONFIG: ./ansible.cfg
  script:
    - ansible-lint -vvv ./*.yml
